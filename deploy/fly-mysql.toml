app = "shadownet-nexus-mysql"
primary_region = "bom"

[build]
  image = "mysql:8.0"

[env]
  MYSQL_DATABASE = "shadownet"
  MYSQL_USER = "shadownet"
  MYSQL_ROOT_HOST = "%"

[mounts]
  source = "mysql_data"
  destination = "/var/lib/mysql"

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
