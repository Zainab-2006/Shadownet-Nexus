# Archived Documentation

Archived on 2026-04-17 after checking active project references.

These files are historical setup, recovery, audit, and planning notes. They were moved here instead of permanently deleted because this folder is not a Git repository and the original files cannot be restored from local version control.

Active tests were not archived. The Cypress specs under `cypress/e2e/` remain part of the configured Cypress spec pattern, and `src/test/storyRuntimeVerification.test.tsx` remains part of the Vitest include pattern.
